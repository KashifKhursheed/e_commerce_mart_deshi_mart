import 'package:flutter/material.dart';

class AuthPageMobile extends StatelessWidget {
  const AuthPageMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
    body: Text("Mobile Auth Page"),
    );
  }
}