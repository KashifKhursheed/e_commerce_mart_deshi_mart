import 'package:flutter/material.dart';

class Homepagemobile extends StatelessWidget {
  const Homepagemobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text("homepage mobile"),);
  }
}