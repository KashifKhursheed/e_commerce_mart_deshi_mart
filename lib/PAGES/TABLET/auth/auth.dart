import 'package:flutter/material.dart';

class AuthPagetablet extends StatelessWidget {
  const AuthPagetablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
    body: Text("web Auth Page"),
    );
  }
}