import 'package:flutter/material.dart';

class AuthPagedesktop extends StatelessWidget {
  const AuthPagedesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("desktop Auth Page"),
    );
  }
}
